# FastMCP Client

`fastmcp-client` is the client-only distribution for FastMCP.

```python
from fastmcp_client import Client
```

Install the full `fastmcp` package when you also need server construction,
in-memory server transports, or multi-server config composition.
